Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
Flask-Migrate>=4.0
Flask-Limiter>=3.5
Werkzeug>=3.0
openpyxl>=3.1

# Only needed if you set DATABASE_URL to a PostgreSQL connection string.
# Safe to leave installed even when using the default SQLite database.
psycopg2-binary>=2.9
